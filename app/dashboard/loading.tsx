import DashboardSkeleton from '../ui/skeletons';

// esto se usa como loading hasta que se cargue la pantalla, haciendo un archivo loading, next lo utiliza automaticamente

export default function Loading() {
  return <DashboardSkeleton />;
}
